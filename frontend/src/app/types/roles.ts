export type roles =
  | "admin"
  | "doctor"
  | "assistant"
  | "worker"
  | "leader"
  | "student";

export enum rolesEnum {
  admin = "admin",
  doctor = "doctor",
  assistant = "assistant",
  worker = "worker",
  leader = "leader",
  student = "student",
}
